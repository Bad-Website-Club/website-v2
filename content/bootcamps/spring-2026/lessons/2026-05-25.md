+++
date = '2026-05-25T15:00:00+00:00'
etz_url = ''
draft = false
title = 'CSS - Basic CSS Part 1'
youtube_id = 'h3ZgepA7XEk'
alternative_recording_urls = []
calendar_ics_url = '/calendars/spring-2026-lessons.ics'
calendar_google_url = 'https://calendar.google.com/calendar/render?action=TEMPLATE&text=CSS+-+Basic+CSS+Part+1&dates=20260525T150000%2F20260525T160000&details=https%3A%2F%2Fbadwebsite.club%2Fbootcamps%2Fspring-2026%2Flessons%2F2026-05-25%2F&location=https%3A%2F%2Fbadwebsite.club%2Fbootcamps%2Fspring-2026%2Flessons%2F2026-05-25%2F&ctz=UTC'
type = 'lessons'
lesson_type = ['theory', 'workshop']
instructors = ['Jess', 'Carmen', 'Eda']
unit = ['Basic CSS']
+++

## Before this session, please:

- Read [What Is CSS: What Is CSS, and What Is Its Role on the Web?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/what-is-css) and take notes
- Read [What Is CSS: What Is the Basic Anatomy of a CSS Rule?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/what-is-the-basic-anatomy-of-a-css-rule) and take notes
- Read [What Is CSS: What Is the Meta Viewport Element Used For?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/what-is-the-meta-viewport-element-used-for) and take notes
- Read [What Is CSS: What Are Some Default Browser Styles Applied to HTML?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/what-are-some-default-browser-styles-applied-to-html) and take notes
- Read [What Is CSS: What Are Inline, Internal, and External CSS, and When Should You Use Each One?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/what-are-inline-internal-and-external-css) and take notes
- Read [What Is CSS: How Do Width and Height Work?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/how-do-width-and-height-work) and take notes
- Read [What Is CSS: What Are the Different Types of CSS Combinators?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/what-are-the-different-types-of-css-combinators) and take notes
- Read [What Is CSS: What Is the Difference Between Inline and Block-Level Elements in CSS?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/what-is-the-difference-between-inline-and-block-level-elements-in-css) and take notes
- Read [What Is CSS: How Does Inline-Block Work, and How Does It Differ from Inline and Block Elements?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/how-does-inline-block-work) and take notes
- Read [What Is CSS: What Are Margins and Padding, and How Do They Work?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/what-are-margins-and-padding) and take notes
- Complete all steps from [Design a Cafe Menu](https://www.freecodecamp.org/learn/responsive-web-design-v9/workshop-cafe-menu/step-1) solo, recording questions and new concepts introduced

## During this session, we'll:

- Review notes
- Review notes
- Review notes
- Review notes
- Review notes
- Review notes
- Review notes
- Review notes
- Review notes
- Review notes
- We'll work through key steps in [Design a Cafe Menu](https://www.freecodecamp.org/learn/responsive-web-design-v9/workshop-cafe-menu/step-1) to build a foundational understanding of CSS. Please finish this workshop solo.

## After this session, please:

- Complete [Design a Business Card](https://www.freecodecamp.org/learn/responsive-web-design-v9/lab-business-card/design-a-business-card) in a creative way that reflects your vibe

## Instructor notes

### [What Is CSS: What Is CSS, and What Is Its Role on the Web?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/what-is-css)

CSS (Cascading Style Sheets) is a markup language to apply styles to HTML. With CSS, we are able to separate the content from its visual presentation.

A common analogy is a webpage as a house. In this analogy, HTML would be the foundation and structure, and CSS would be the paint and decorations.

To apply styles to HTML elements, we _select_ those elements using CSS.

CSS also allows us to create responsive designs so that our websites can look good on any device independent of screen size.

Since styles are _cascading_, it means they can be overridden or inherited.

We can keep our styles in a separate file using external stylesheets which helps with modularity and maintainability of our websites.

### [What Is CSS: What Is the Basic Anatomy of a CSS Rule?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/what-is-the-basic-anatomy-of-a-css-rule)

We declare styles for HTML elements using what is called a **CSS rule**. It has two parts:
- a **selector**: the pattern used to identify the HTML element
- a **declaration** block: inside of which we have a series of declarations, each of which having a **property** and a **value**.

Some of the basic selectors are type selectors, class selectors, and id selectors.

For example, a selector can be a type selector, such as `button`. An example for property can be `background-color`, and the value, `purple`:

```css
button {
  background-color: purple;
}
```

Property names are followed by a colon (`:`), and each declaration ends with a semicolon (`;`).

All id selectors start with a hash symbol (`#`).
All class selectors start with a dot (`.`).

We can apply a style to multiple elements, separating them with a comma:

```css
#title,
.subheading {
  color: navy;
}
```

### [What Is CSS: What Is the Meta Viewport Element Used For?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/what-is-the-meta-viewport-element-used-for)

The meta viewport element is a special HTML `meta` element to instruct the browser to control the dimensions and scaling of a page on different devices. In other words, it instructs the browser on how the viewport should be sized. It's very important for making our web pages responsive.

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0">
```

Here's what each part of the value of `content` means:
- `width=device-width` instructs the browser to set the width of the page to match the screen width of the device.
- `initial-scale=1.0` sets the initial zoom level as `100%` when the page is first loaded.

Without this element, the browsers usually render the pages in desktop screen width and scale it down, which is not easily readable for small screens.

Since this element allows us to control the zooming, we should **avoid using `user-scalable=no`** like this:

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
```

As many users rely on zoom for better readability, we should ensure that our pages can stay responsive.

### [What Is CSS: What Are Some Default Browser Styles Applied to HTML?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/what-are-some-default-browser-styles-applied-to-html)

Default browser styles ("user-agent styles") provide basic formatting and styles to HTML elements. They can vary from browser to browser.

Some examples are:
- Headings (`h1` through `h6`). The `h1` element is usually rendered as bold and bigger than the other heading elements. 
- The `hr` element. Browsers apply a simple line.
- The `blockquote` element. Browsers add indentation to set them apart from the rest of the text.
- The anchor (`<a>`) element. It's usually rendered with blue color and underlined.
- List elements. Browsers usually add indentation, and bullet points to unordered lists (`ul`) and numbering to ordered lists (`ol`).

### [What Is CSS: What Are Inline, Internal, and External CSS, and When Should You Use Each One?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/what-are-inline-internal-and-external-css)

We can apply CSS to our page usually in three ways:
- inline
- internal
- external

**Inline CSS** is written on an HTML element using the `style` attribute.

```html
<p style="color: green;">This is an inline-styled paragraph.</p>
```

This option takes precedence, and overrides other styles for the element. We should avoid using inline CSS for keeping our structure and styles separate and our code maintainable.

**Internal CSS** is written using `style` tags inside the `head` tags in HTML.

```html
<head>
  <style>
    p {
      color: blue;
    }
  </style>
</head>
<body>
  <p>This paragraph is styled using internal CSS.</p>
</body>
```

It can be useful for single-page websites when the styles are not going to be reused elsewhere. But it's still not the best option in terms of modularity and maintainability as it mixes HTML and CSS in one file.

**External CSS** is written in a separate file with the extension `.css` that is linked to the HTML document with the `link` element inside the `head` tags.

```html
<head>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <p>This paragraph is styled using external CSS.</p>
</body>
```

In this example, `styles.css` can include:

```css
p {
  color: red;
}
```

It's ideal for reusability and to maintain a consistent style across pages.

### [What Is CSS: How Do Width and Height Work?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/how-do-width-and-height-work)

The `width` and `height` properties control the dimensions of elements. The values can be in different units such as pixels (`px`), percentages (`%`), viewport units (`vw`, `vh`), etc.

When it's not specified, the default `width` and `height` are set to `auto`. In that case, browser determines it based on the content, the parent element and display type.

The `min-width` and `min-height` properties specify the minimum width and height an element can be, so that it's not shorter than the set value.

If `min-width` or `min-height` are larger than `width` and `height`, they will override those values. For example, even though the `width` and `height` are set to `50px`, the `div` element will have `100px` width and height as specified by `min-width` and `min-height` elements:

```html
<head>
  <style>
    .box {
      width: 50px;
      min-width: 100px;
      height: 50px;
      min-height: 100px;
      background-color: lightcoral;
    }
  </style>
</head>
<body>
  <div class="box"></div>
</body>
```

The `max-width` and `max-height` properties specify the maximum width and height an element can be, regardless of the content size.

If `max-width` or `max-height` are smaller than `width` and `height`, they will override those values. For example, even though the `width` and `height` are set to `200px`, the `div` element will have `150px` width and height as specified by `max-width` and `max-height` elements:

```html
<head>
  <style>
    .box {
      width: 200px;
      max-width: 150px;
      height: 200px;
      max-height: 150px;
      background-color: lightgreen;
    }
  </style>
</head>
<body>
  <div class="box"></div>
</body>
```

### [What Is CSS: What Are the Different Types of CSS Combinators?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/what-are-the-different-types-of-css-combinators)

With CSS combinators, we can select elements based on their relationship to other elements.

The **descendant combinator**, represented by a single space (` `), character helps us select elements that are matched by the second selector if they have an ancestor element matching the first selector:

```html
<link rel="stylesheet" href="styles.css">

<figure>
  <img src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/relaxing-cat.jpg" alt="Relaxing Cat">
  <figcaption>A relaxing cat with a border</figcaption>
</figure>
```

```css
figure img {
  border: 4px solid red;
}
```

In this case, border will be applied to the `img` element.

The **child combinator**, represented by the character `>`, helps us select elements that are direct children of the specified parent element.

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <p>First</p>
  <div>
    <p>Second</p>
  </div>
  <div>
    <p>Third</p>
  </div>
</div>
```

```css
.container > p {
  color: blue;
}
```

In this case, the color will be applied only to the paragraph with the text `First` because it's the only direct child of the `div` with the class `container`.

The **next-sibling combinator**, represented with the character `+`, helps us select the element that immediately follows a specified sibling element.

```html
<link rel="stylesheet" href="styles.css">

<figure>
  <img
    src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/relaxing-cat.jpg"
    alt="A cute orange cat lying on its back."
  />
  <figcaption>A cute orange cat lying on its back.</figcaption>
</figure>
```

```css
img + figcaption {
  border: 4px solid black;
}
```

In this case, the border will be applied to the `figcaption` element.

The subsequent-sibling combinator, represented by the character `~`, helps us select the elements that immediately follow a specified sibling element.

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <h2>Subheading</h2>
  <p>First paragraph.</p>
  <p>Second paragraph.</p>
  <p>Third paragraph.</p>
  <p>Another paragraph element</p>
</div>
```

```css
h2 ~ p {
  color: green;
}
```

In this case, the color will be applied to all the paragraph elements following `h2`.

### [What Is CSS: What Is the Difference Between Inline and Block-Level Elements in CSS?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/what-is-the-difference-between-inline-and-block-level-elements-in-css)

An HTML element is either an **inline** or **block-level** element. This affects how it's displayed on the page.

Block-level elements:
- take up the full width available and stretch across the width of their container.
- always start on a new line and push other content to the next line.
- have the CSS property `display: block;` by default.

Some example block-level elements are:
- `div`s
- paragraphs
- headings
- ordered and unordered lists
- section elements

Inline elements:
- only take up as much width as they need and don't start on a new line.
- flow alongside the other content.
- have the CSS property `display: inline;` by default.

Some example inline elements are:
- `span`
- `anchor`
- `img`

### [What Is CSS: How Does Inline-Block Work, and How Does It Differ from Inline and Block Elements?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/how-does-inline-block-work)

Aside from `inline` and `block` elements, there are also `inline-block` elements. 

Like inline elements, they flow alongside the other content and don't start on a new line. But unlike inline elements, they can have their width and height specified. That is, we can control the size of an `inline-block` element.

### [What Is CSS: What Are Margins and Padding, and How Do They Work?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-what-is-css/what-are-margins-and-padding)

Margins are useful for defining layout structure. With margins, we can control the space outside an element and separate it from other elements.

Paddings allow us to control the space inside an element. They improve readability and provide better aesthetic appeal. 

The `margin` property is a shorthand to define the properties `margin-top`, `margin-right`, `margin-bottom` and `margin-left`. (_It goes clockwise._)

```css
p {
  margin: 10px 5px 20px 4px;
}
```

In this case, the `p` element will have `10px` margin from the top, `5px` margin from the right, `20px` margin from the bottom, and `4px` margin from the left. If we are to write it separately, it might look like this:

```css
p {
  margin-top: 10px;
  margin-right: 5px;
  margin-bottom: 20px;
  margin-left: 4px;
}
```

If the top and bottom, left and right will have the same value, the shorthand can include just two values:

```css
p {
  margin: 10px 5px;
}
```

In this case, the `p` element will have `10px` margin from the top and the bottom, and `5px` margin from the left and the right.

The `padding` property is also very similar to `margin`. 
It's a shorthand to define the properties `padding` properties are `padding-top`, `padding-right`, `padding-bottom` and `padding-left`. (_It also goes clockwise._)

Note that the padding of an element is the space between its content and its border.

```css
section {
  padding: 8px 4px;
}
```

In this case, the `section` element will have `8px` padding from the top and the bottom, and `4px` padding from the left and the right.

### [Design a Cafe Menu](https://www.freecodecamp.org/learn/responsive-web-design-v9/workshop-cafe-menu/step-1)

We'll take a look at this workshop to practice the basics of CSS!
