+++
date = '2026-05-27T15:00:00+00:00'
etz_url = ''
draft = false
title = 'CSS - Basic CSS Part 2'
youtube_id = 'aT5rCwYkipc'
alternative_recording_urls = []
calendar_ics_url = '/calendars/spring-2026-lessons.ics'
calendar_google_url = 'https://calendar.google.com/calendar/render?action=TEMPLATE&text=CSS+-+Basic+CSS+Part+2&dates=20260527T150000%2F20260527T160000&details=https%3A%2F%2Fbadwebsite.club%2Fbootcamps%2Fspring-2026%2Flessons%2F2026-05-27%2F&location=https%3A%2F%2Fbadwebsite.club%2Fbootcamps%2Fspring-2026%2Flessons%2F2026-05-27%2F&ctz=UTC'
type = 'lessons'
lesson_type = ['theory', 'review', 'quiz']
instructors = ['Jess', 'Carmen', 'Eda']
unit = ['Basic CSS']
+++

## Before this session, please:

- Read [CSS Specificity, the Cascade Algorithm, and Inheritance: What Is CSS Specificity, and the Specificity for Inline, Internal, and External CSS?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-css-specificity-the-cascade-algorithm-and-inheritance/what-is-css-specificity-and-the-specificity-for-inline-internal-and-external-css) and take notes
- Read [CSS Specificity, the Cascade Algorithm, and Inheritance: What Is the Universal Selector, and What Is Its Specificity?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-css-specificity-the-cascade-algorithm-and-inheritance/what-is-the-universal-selector) and take notes
- Read [CSS Specificity, the Cascade Algorithm, and Inheritance: What Is the Specificity for Type Selectors?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-css-specificity-the-cascade-algorithm-and-inheritance/what-is-the-specificity-for-type-selectors) and take notes
- Read [CSS Specificity, the Cascade Algorithm, and Inheritance: What Is the Specificity for Class Selectors?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-css-specificity-the-cascade-algorithm-and-inheritance/what-is-the-specificity-for-class-selectors) and take notes
- Read [CSS Specificity, the Cascade Algorithm, and Inheritance: What Is the Specificity for ID Selectors?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-css-specificity-the-cascade-algorithm-and-inheritance/what-is-the-specificity-for-id-selectors) and take notes
- Read [CSS Specificity, the Cascade Algorithm, and Inheritance: What Is the important Keyword, and What Are the Best Practices for Using It?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-css-specificity-the-cascade-algorithm-and-inheritance/what-is-the-important-keyword) and take notes
- Read [CSS Specificity, the Cascade Algorithm, and Inheritance: How Does the Cascade Algorithm Work at a High Level?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-css-specificity-the-cascade-algorithm-and-inheritance/how-does-the-cascade-algorithm-work-at-a-high-level) and take notes
- Read [CSS Specificity, the Cascade Algorithm, and Inheritance: How Does Inheritance Work with CSS at a High Level?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-css-specificity-the-cascade-algorithm-and-inheritance/how-does-inheritance-work-with-css-at-a-high-level) and take notes
- Read [CSS Fundamentals Review](https://www.freecodecamp.org/learn/responsive-web-design-v9/review-basic-css/review-basic-css) and take notes
- Try [CSS Fundamentals Quiz](https://www.freecodecamp.org/learn/responsive-web-design-v9/quiz-basic-css/quiz-basic-css) solo to test your understanding of CSS fundamentals

## During this session, we'll:

- Review notes
- Review notes
- Review notes
- Review notes
- Review notes
- Review notes
- Review notes
- Review notes
- Review notes

## After this session, please:

- Read [Styling Lists and Links: How Do You Space List Items Using margin or line-height?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-styling-lists-and-links/how-do-you-space-list-items-using-margin-or-line-height) and take notes
- Read [Styling Lists and Links: How Do the Different list-style Properties Work?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-styling-lists-and-links/how-do-the-different-list-style-properties-work) and take notes
- Read [Styling Lists and Links: Why Are Default Link Styles Important for Usability on the Web?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-styling-lists-and-links/why-are-default-link-styles-important-for-usability-on-the-web) and take notes
- Read [Styling Lists and Links: How Do You Style the Different Link States?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-styling-lists-and-links/how-do-you-style-the-different-link-states) and take notes
- Complete [Build a Stylized To-Do List](https://www.freecodecamp.org/learn/responsive-web-design-v9/lab-stylized-to-do-list/build-a-stylized-to-do-list) over the next few days

## Instructor notes

### [CSS Specificity, the Cascade Algorithm, and Inheritance: What Is CSS Specificity, and the Specificity for Inline, Internal, and External CSS?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-css-specificity-the-cascade-algorithm-and-inheritance/what-is-css-specificity-and-the-specificity-for-inline-internal-and-external-css)

When multiple styles are applied to an element, we need to determine which one comes first. This is where CSS specificity comes in.

CSS specificity is determined based on the type of selectors.

We can think of calculating specificity as a four-part value `(a, b, c, d)`.

(You might usually see this as a [three-column comparison](https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Cascade/Specificity#three-column_comparison), but we'll stick with four-part value for now.)

Each value can be either `1` or `0`. The weight (importance) of values goes from left to right (from `a` to `d`).

Inline styles have the highest specificity...

```html
<p style="color: purple">…</p>
```

...therefore, they are the value `a`, and contributes 1 to the total value.

The number of ID selectors is `b`, it contributes 1 to the value.

The number of class selectors, attribute selectors, and pseudo-classes is `c`, it contributes 1 to the value.

The number of type selectors, pseudo-elements, and universal selectors is `d`, it contributes 1 to the value.

Since universal selectors have the lowest specificity, they only contribute 0 to the value.

This might sound a little complicated, so let's look at an example.
Given this HTML:

```html
<body class="bodyClass">
  <section class="sectionClass">
    <div class="parentClass">
      <p id="myElement">Hey!</p>
    </div>
  </section>
</body>
```

Instead of using all the classes and an attribute selector, selecting the `p` element by ID will have a higher specificity:

```css
#myElement {
  color: green; /* 0-1-0-0  - WINS! */
}
.bodyClass .sectionClass .parentClass [id="myElement"] {
  color: yellow; /* 0-0-4-0 */
}
```
_CSS example from [MDN](https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Cascade/Specificity#three-column_comparison) (adapted for four-part specificity)._

Internal CSS and external CSS have the same level of specificity, it's determined by the number of selectors.

Also, because of the cascading nature of CSS, if two rules have the same specificity, the one that appears later in the document wins.

### [CSS Specificity, the Cascade Algorithm, and Inheritance: What Is the Universal Selector, and What Is Its Specificity?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-css-specificity-the-cascade-algorithm-and-inheritance/what-is-the-universal-selector)

The universal selector (`*`) matches any element in the document.

It can be useful to reset styles because we can apply a style to all elements on the page with it.

A common use is setting the `margin` and `padding` to `0` for the whole document:

```css
* {
  margin: 0;
  padding: 0;
}
```

It comes as the lowest in the specificity hierarchy and contributes 0 to the specificity value, which means that any selector will override the styles set by a universal selector.

Let's say there exists only a `p` element in our HTML:

```html
<p>Hey!</p>
```

Using the universal selector to change its color will work...
```css
* {
  color: green;
}
```

...but it will be overridden by any selector:
```css
* {
  color: green;
}

p {
  color: purple; /* WINS */
}
```

### [CSS Specificity, the Cascade Algorithm, and Inheritance: What Is the Specificity for Type Selectors?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-css-specificity-the-cascade-algorithm-and-inheritance/what-is-the-specificity-for-type-selectors)

Type selectors are used to select elements based on their tag name and have the lowest specificity compared to most other selectors.

### [CSS Specificity, the Cascade Algorithm, and Inheritance: What Is the Specificity for Class Selectors?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-css-specificity-the-cascade-algorithm-and-inheritance/what-is-the-specificity-for-class-selectors)

Class selectors are used to select elements that have the same class name, and while they have higher specificity than type selectors, they have lower specificity than ID selectors and inline styles.

### [CSS Specificity, the Cascade Algorithm, and Inheritance: What Is the Specificity for ID Selectors?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-css-specificity-the-cascade-algorithm-and-inheritance/what-is-the-specificity-for-id-selectors)

ID selectors are used to select elements that are guaranteed to be unique, and they have the highest specificity among selectors. But, since they have lower specificity than inline styles, they will be overridden by them.

### [CSS Specificity, the Cascade Algorithm, and Inheritance: What Is the important Keyword, and What Are the Best Practices for Using It?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-css-specificity-the-cascade-algorithm-and-inheritance/what-is-the-important-keyword)

The `!important` keyword forces the browser to apply a specific style, disregarding the specificity value.

It means that even the inline styles can be overridden!

```html
<head>
  <style>
    p {
      color: green !important;
    }
  </style>
</head>
<body>
  <p style="color: purple;">Hey!</p> <!-- This will be green! -->
</body>
```

It doesn't change the specificity value of the selector, but just ensure that the style is applied. 

We should avoid using it as much as possible because it can cause unintended behavior or difficulties in maintaining our CSS.

### [CSS Specificity, the Cascade Algorithm, and Inheritance: How Does the Cascade Algorithm Work at a High Level?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-css-specificity-the-cascade-algorithm-and-inheritance/how-does-the-cascade-algorithm-work-at-a-high-level)

Browsers use the Cascade Algorithm to determine which CSS rules to apply when there are multiple styles for the same element.

The process of the Cascade Algorithm goes like this:
1. The browser finds styles that apply to the element by filtering out other rules. (**relevance**)
2. The origin source is considered. It can be the browser's default styles (user-agent), styles set by the user, and the styles written by the author. (**origin**)
3. The importance of each rule is evaluated. Priority is given to the rules with the `!important` keyword. (**importance**)
4. The rules are applied according to specificity. (**specificity**)
5. If the rules have the same specificity, styles are applied based on the order of appearance of the rule. The rule that comes later is applied. (**order of appearance**)

### [CSS Specificity, the Cascade Algorithm, and Inheritance: How Does Inheritance Work with CSS at a High Level?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-css-specificity-the-cascade-algorithm-and-inheritance/how-does-inheritance-work-with-css-at-a-high-level)

Child elements can inherit certain properties from their parent elements. Some properties are inherited by default, such as `color`, `font-family`, `line-height`, etc.

Properties such as `margin`, `padding`, `border`, and `background` are not inherited by default. To inherit these styles, we have to use the `inherit` keyword on a child element.

Inheritance is useful for reusability and consistency. For example, instead of defining the same style for multiple child elements, we can define it once for the parent element and let the children inherit it.

### [CSS Fundamentals Review](https://www.freecodecamp.org/learn/responsive-web-design-v9/review-basic-css/review-basic-css)

We've been learning a lot of CSS fundamentals, and these are the topics we've seen so far:

- CSS Basics
- Inline, Internal, and External CSS
- Working With the `width` and `height` Properties
- Different Types of CSS Combinators
- Inline, Block, and Inline-Block Level Elements
- Margin and Padding
- CSS Specificity
