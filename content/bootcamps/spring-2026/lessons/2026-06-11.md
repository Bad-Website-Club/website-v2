+++
date = '2026-06-11T15:00:00+00:00'
etz_url = ''
draft = false
title = 'CSS - The Box Model'
youtube_id = 'N_dKcfPkomo'
alternative_recording_urls = []
calendar_ics_url = '/calendars/spring-2026-lessons.ics'
calendar_google_url = 'https://calendar.google.com/calendar/render?action=TEMPLATE&text=CSS+-+The+Box+Model&dates=20260611T150000%2F20260611T160000&details=https%3A%2F%2Fbadwebsite.club%2Fbootcamps%2Fspring-2026%2Flessons%2F2026-06-11%2F&location=https%3A%2F%2Fbadwebsite.club%2Fbootcamps%2Fspring-2026%2Flessons%2F2026-06-11%2F&ctz=UTC'
type = 'lessons'
lesson_type = ['theory', 'workshop', 'lab', 'review', 'quiz']
instructors = ['Jess', 'Carmen', 'Eda']
unit = ['The Box Model']
+++

## Before this session, please:

- Read [Working with CSS Transforms, Overflow, and Filters: What Is Overflow in CSS, and How Does It Work?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-working-with-css-transforms-overflow-and-filters/what-is-overflow-in-css) and take notes
- Read [Working with CSS Transforms, Overflow, and Filters: What Is the CSS Transform Property, and How Does It Work?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-working-with-css-transforms-overflow-and-filters/what-is-the-css-transform-property) and take notes
- Read [Working with CSS Transforms, Overflow, and Filters: What Is the CSS Box Model, and How Does It Work?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-working-with-css-transforms-overflow-and-filters/what-is-the-css-box-model) and take notes
- Read [Working with CSS Transforms, Overflow, and Filters: What Is Margin Collapsing, and How Does It Work?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-working-with-css-transforms-overflow-and-filters/what-is-margin-collapsing) and take notes
- Read [Working with CSS Transforms, Overflow, and Filters: What Is the Difference Between content-box and border-box?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-working-with-css-transforms-overflow-and-filters/what-is-the-difference-between-content-box-and-border-box) and take notes
- Read [Working with CSS Transforms, Overflow, and Filters: What Is a CSS Reset, and What Are Some Common Examples?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-working-with-css-transforms-overflow-and-filters/what-is-a-css-reset) and take notes
- Read [Working with CSS Transforms, Overflow, and Filters: What Is the CSS Filter Property, and What Are Common Examples?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-working-with-css-transforms-overflow-and-filters/what-is-the-css-filter-property) and take notes
- Complete all steps from [Design a Rothko Painting](https://www.freecodecamp.org/learn/responsive-web-design-v9/workshop-rothko-painting/step-1) solo over the next few days
- Complete [Build a Confidential Email Page](https://www.freecodecamp.org/learn/responsive-web-design-v9/lab-confidential-email-page/build-a-confidential-email-page) solo over the next few days
- Read [CSS Layouts and Effects Review](https://www.freecodecamp.org/learn/responsive-web-design-v9/review-css-layout-and-effects/review-css-layout-and-effects) and take notes
- Complete [CSS Layouts and Effects Quiz](https://www.freecodecamp.org/learn/responsive-web-design-v9/quiz-css-layout-and-effects/quiz-css-layout-and-effects) solo to test your knowledge of CSS layouts

## During this session, we'll:

- Review notes
- Review notes
- Review notes
- Review notes
- Review notes
- Review notes
- Review notes
- We'll preview some of the steps from [Design a Rothko Painting](https://www.freecodecamp.org/learn/responsive-web-design-v9/workshop-rothko-painting/step-1) together, asking you to complete this workshop solo over the weekend
- We'll talk about [Build a Confidential Email Page](https://www.freecodecamp.org/learn/responsive-web-design-v9/lab-confidential-email-page/build-a-confidential-email-page) together briefly, but you'll working to complete this solo over the next few days
- Review notes

## After this session, please:

- Read [Working with CSS Flexbox: What Is CSS Flexbox, and When Should You Use It?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-working-with-css-flexbox/what-is-css-flexbox) and take notes
- Read [Working with CSS Flexbox: What Are Some Common Flex Properties, and How Do They Work?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-working-with-css-flexbox/what-are-some-common-flex-properties) and take notes
- Try to complete all the steps from [Build a Flexbox Photo Gallery](https://www.freecodecamp.org/learn/responsive-web-design-v9/workshop-flexbox-photo-gallery/step-1) to begin to get an understanding of flexbox
- Try to complete as much of [Design a Set of Colorful Boxes](https://www.freecodecamp.org/learn/responsive-web-design-v9/workshop-colorful-boxes/step-1) as you can, to reinforce your understanding of flexbox. You may need to finish this over the next few days
- Complete [Design a Pricing Plans Layout Page](https://www.freecodecamp.org/learn/responsive-web-design-v9/lab-pricing-plans-layout/design-a-pricing-plans-layout-page) solo over the next few days
- Read [CSS Flexbox Review](https://www.freecodecamp.org/learn/responsive-web-design-v9/review-css-flexbox/review-css-flexbox) and take notes
- Complete [CSS Flexbox Quiz](https://www.freecodecamp.org/learn/responsive-web-design-v9/quiz-css-flexbox/quiz-css-flexbox) solo to test your knowledge of flexbox
- Spend the next few days or weeks completing [Build a Page of Playing Cards](https://www.freecodecamp.org/learn/responsive-web-design-v9/lab-page-of-playing-cards/build-a-page-of-playing-cards). Certification projects should show as much of your skill and style as possible.

## Instructor notes

### [Working with CSS Transforms, Overflow, and Filters: What Is Overflow in CSS, and How Does It Work?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-working-with-css-transforms-overflow-and-filters/what-is-overflow-in-css)

An element can overflow, meaning that its size can exceed the size of its container element.

An overflow can be in two dimensions:
- along the x-axis (horizontal)
- along the y-axis (vertical)

We can control the overflow behavior with properties like `overflow`, `overflow-x`, and `overflow-y`.

### [Working with CSS Transforms, Overflow, and Filters: What Is the CSS Transform Property, and How Does It Work?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-working-with-css-transforms-overflow-and-filters/what-is-the-css-transform-property)

We can apply various transformations to elements, such as rotating, scaling, skewing, or translating in 2D or 3D space.

The `transform` property applies a mathematical transformation to an element's coordinate system. It allows us to change its shape and position while keeping the document flow intact.

We can move an element with the `translate` function, which can take values for the x-axis and the y-axis.

This will be `50px` to the right, `100px` down from its original position:

```css
transform: translate(50px, 100px);
```

We can rotate an element around a fixed point with the `rotate` function, which takes a value representing the angle of the rotation.

This will be rotated 45 degrees clockwise:

```css
transform: rotate(45deg);
```

We can change the size of an element with the `scale` function, which takes values for horizontal scaling and vertical scaling.

This will make it half times wider and twice as tall:

```css
transform: scale(1.5, 2);
```

The values to `transform` property can be combined, such as:

```css
transform: translate(50px, 100px) rotate(45deg) scale(1.5, 2);
```

There are some accessibility concerns we should be mindful of:
- If we rearrange the order of elements with `transform`, it won't be reflected for users with screen readers, as screen readers will still read the content as defined in the original DOM order.
- We should avoid using `scale` for text resizing as it may overflow its container.
- Animations can be triggering for some users, so if `transform` is used for animating elements, we should provide a way to reduce or turn off animations.
- For elements hidden by the `transform` property, we should make sure the content is still accessible to screen reader users. (To truly hide content, we can use `display: none;` or `visibility: hidden;`.)
- We should make sure that interactive elements that use the `transform` property are still intuitive and remain interactive.

### [Working with CSS Transforms, Overflow, and Filters: What Is the CSS Box Model, and How Does It Work?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-working-with-css-transforms-overflow-and-filters/what-is-the-css-box-model)

The CSS Box Model is a way to define how HTML elements are structured.

Every element is surrounded by a box. There are four components to the box:
- **the content area** (the innermost area of the element)
- **the padding** (the space between the content and border)
- **the border** (the outer edge of the element)
- **the margin** (the space outside the border of the element)

We can define the padding, border, and margin with CSS properties we've seen before, such as `border`, `padding`, and `margin`!

### [Working with CSS Transforms, Overflow, and Filters: What Is Margin Collapsing, and How Does It Work?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-working-with-css-transforms-overflow-and-filters/what-is-margin-collapsing)

There are some cases when margins of two elements can collapse, resulting in a single margin.

**Adjacent elements**: When the vertical margins of neighboring elements overlap, it will result in a single margin, which is the larger of the two.

For example, in a case where an element has `20px` bottom margin and the other has `30px` top margin (two elements being stacked vertically), the resulting margin between them will be `30px` (instead of adding them together, resulting in `50px`).

```html
<div class="box1">Box 1</div>
<div class="box2">Box 2</div>
```

```css
.box1 {
  margin-bottom: 20px;
}

.box2 {
  margin-top: 30px;
}
```

**Parent and first/last child**: When there's no border, padding, or content in the parent to separate the parent's margin from the child's, their margins will collapse.

For example, if the "empty" parent has `40px` top margin, and the child has `30px` top margin, the result will be `40px` instead of `70px`.

```html
<div class="parent">
  <div class="child">Child element</div>
</div>
```

```css
.parent {
  margin-top: 40px;
}

.child {
  margin-top: 30px;
}
```

**An "empty" element**: When an element has no content, padding, or border, its top and bottom margins can collapse.

For example, if an empty `div` that has no height has different top and bottom margins, the resulting "empty" space will be that of the larger margin. In the example below, it will be `20px`:

```html
<div class="empty"></div>
```

```css
.empty {
  margin-top: 20px;
  margin-bottom: 10px;
  height: 0;
}
```

### [Working with CSS Transforms, Overflow, and Filters: What Is the Difference Between content-box and border-box?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-working-with-css-transforms-overflow-and-filters/what-is-the-difference-between-content-box-and-border-box)

The `box-sizing` property in CSS can have two values:

- `content-box`
- `border-box`

With `content-box`, the width and height we set for the element applies to the content area only, not including the padding, border, and margin.

With `border-box`, the width and height we set include the element's content, padding, and border (not margin). This is more helpful for responsive layouts because the element's total size stays fixed when the padding or border changes.

### [Working with CSS Transforms, Overflow, and Filters: What Is a CSS Reset, and What Are Some Common Examples?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-working-with-css-transforms-overflow-and-filters/what-is-a-css-reset)

To remove the default styling that browsers apply to HTML elements, we can use a CSS reset, which is helpful for improving consistency across browsers.

We can define custom CSS resets as well as use a third-party CSS reset.

A simple example for a starting point is removing the margin and paddings of all elements:

```css
* {
  margin: 0;
  padding: 0;
}
```

Some of the third-party CSS resets are:
- [Normalize.css](https://necolas.github.io/normalize.css/)
- [sanitize.css](https://csstools.github.io/sanitize.css/)

We should also be mindful of accessibility when using CSS resets, and avoid resetting styles that might be helpful for assistive technologies.

### [Working with CSS Transforms, Overflow, and Filters: What Is the CSS Filter Property, and What Are Common Examples?](https://www.freecodecamp.org/learn/responsive-web-design-v9/lecture-working-with-css-transforms-overflow-and-filters/what-is-the-css-filter-property)

We can define effects such as blurring, color shifting, and contrast adjustments for an element using the `filter` property.

The `blur` function applies a Gaussian blur, it takes in the amount in pixels which represents the radius of the blur.

```css
filter: blur(2px);
```

The `brightness` function adjusts the brightness of the element. For example, the value of `0%` will make the element completely black, and the values over `100%` will increase the brightness.

```css
filter: brightness(150%);
```

The `grayscale` function converts the element to grayscale. It takes a value which is the amount in percentages. For example, `100%` makes it completely grayscale.

```css
filter: grayscale(100%);
```

The `sepia` function applies a sepia tone to an element. Similar to `grayscale`, it takes a value which is the amount of percentages.

```css
filter: sepia(80%);
```

The `hue-rotate` function applies a hue rotation to an element. It takes a value in degrees, representing a rotation around the color circle.

```css
filter: hue-rotate(90deg);
```

The values to `filter` property can be combined, such as:

```css
filter: brightness(150%) sepia(80%);
```

There are also other functions such as `contrast`, `invert`, and `saturate`.

### [Design a Rothko Painting](https://www.freecodecamp.org/learn/responsive-web-design-v9/workshop-rothko-painting/step-1)

In this fun exercise, we'll practice our knowledge of the CSS box model!
### [Build a Confidential Email Page](https://www.freecodecamp.org/learn/responsive-web-design-v9/lab-confidential-email-page/build-a-confidential-email-page)

We'll take a look at this exercise that goes over what we've learned so far.

### [CSS Layouts and Effects Review](https://www.freecodecamp.org/learn/responsive-web-design-v9/review-css-layout-and-effects/review-css-layout-and-effects)

In this unit, we've covered a lot of ground! Some of the concepts we've looked at are:
- CSS `overflow` property
- CSS `transform` property
- The box model
- Margin collapse
- The `content-box` and `border-box` values
- CSS resets
- CSS `filter` Property
